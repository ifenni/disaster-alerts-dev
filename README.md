# disaster-alerts
Cron-friendly alerts from NWS/USGS with email via yagmail.

## Quick start
- Create conda env from `environment.yml`
- Add SMTP creds to `.env`
- Configure `config/*.yaml`
- Point cron to `scripts/run.sh`
