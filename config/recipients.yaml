# config/recipients.yaml
# Routing groups → recipient email lists.
# Events set a `routing_key` (e.g., "default", "severe", "ops"); the pipeline
# sends one email per group to the addresses listed here.

default:
  - you@example.com

# severe:
#   - ops-team@example.com
#   - incident-manager@example.com

# ops:
#   - noc@example.com
