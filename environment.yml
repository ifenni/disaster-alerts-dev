# environment.yml
name: disaster-alerts
channels:
  - conda-forge
channel_priority: strict
dependencies:
  - python>=3.11
  - flask
  - pydantic
  - pyyaml
  - requests
  - pytest
  - shapely
  - folium
  - pip
  - pip:
      - yagmail
